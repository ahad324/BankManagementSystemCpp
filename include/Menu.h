#ifndef MENU_H
#define MENU_H

#include <string>
#include <vector>
#include <iostream>

using namespace std;

void ShowMenuOptions(const vector<string> &options);
void InputTaking(const string &prompt);
void start();

#endif